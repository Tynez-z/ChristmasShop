<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.christmasshop">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChristmasShop">
        <activity android:name=".LightsDetailActivity"></activity>
        <activity android:name=".ToysDetailActivity" />
        <activity android:name=".TreeDetailActivity" />
        <activity android:name=".NYlightsActivity" />
        <activity android:name=".NYtoysActivity" />
        <activity android:name=".NYTreeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>